export interface Movie {
    type: string;
    id: number;
    title: string;
    popularity?: number;
    release_date?: Date;
    poster_path?: string;
    backdrop_path?: string;
}

// export interface MovieDetail {

// }
